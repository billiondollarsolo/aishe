# AIShe assets

- **Canonical product art for README/GitHub:** `aishe-banner.png` (glasses mark + **AIShe** + **AI Shell**).
- SVG files (`aishe-logo.svg`, `aishe-banner.svg`, `aishe-icon.svg`) are secondary; titles/aria use **AIShe**. Prefer updating the PNG when the mark changes.
- CLI package name remains `aishe`; env vars `AISHE_*`.

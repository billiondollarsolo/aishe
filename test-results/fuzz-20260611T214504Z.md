# aishe zsh-PTY fuzz report

- Date: 20260611T214504Z
- Binary: `target/release/aishe`
- Scale: 10   Seed: 1234
- Duration: 1999.8s
- Result: **PASS** (3219 cases)

Drives the real `aishe zsh` front-end through a pseudo-terminal with a deterministic fake model. After every case it asserts the shell is alive, routing is correct, and none of these ever leak: `parse error`, `(eval):`, `no matches found`, `command not found: #`, `command not found: ?`, `bad pattern`, `bad set of key`, `bad math expression`.

## Cases by category

| Category | Passed | What it checks |
| --- | ---: | --- |
| `command` | 1200 | real shell commands (pipes, redirects, globs, quoting, control structures, env vars), output matched exactly |
| `nl-sigil` | 2000 | `?`/`#`-forced natural language stuffed with shell metacharacters, must reach the AI and never reach zsh's parser |
| `adv-cmd` | 16 | adversarial model responses (prose, malformed commands), must be surfaced as answers, never eval'd |
| `adv-danger` | 3 | dangerous valid commands, must be held for review, never run |

## Example generated inputs

**command**

```
echo {1..3}_M_57767
echo M_90844 > /tmp/aishe_fuzz_$$ && cat /tmp/aishe_fuzz_$$
echo M_02076
echo fwd aqp | tr a-z A-Z
echo M_08462 | cat
printf '%s\n' M_67507
```

**nl-sigil**

```
#time 's'
?who "q" czn
# how hxn adcfu |z jglcuu
# why foo>bar bxqgb
# which <x> >y $(date) **
? can you shja ? umddy sbwx ll
```

**adv-cmd**

```
? explain
```

**adv-danger**

```
? do the risky thing
```

